 $(".modal").modal('hide')

 $('table tbody')
.html("<%= j render 'expenses', { expenses: @expenses } %>");


$('#total')
.text("$ " + "<%= j render 'total', { expenses: @expenses } %>");

$('#count')
.text("<%= j render 'count', { expenses: @expenses } %>");

$('#average')
.text("$ " + "<%= j render 'average', { expenses: @expenses } %>");
